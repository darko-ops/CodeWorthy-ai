{
  "name": "codeworthy-steward",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "CodeWorthy Steward — the hosted GitHub App: safe-mechanics, branch protection for non-engineers, append-only audit log, and an advise-only AI reviewer. It never merges, never force-pushes, never rewrites history.",
  "scripts": {
    "start": "node src/server.mjs",
    "test": "node --test test/"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "pg": "^8.13.1"
  }
}
