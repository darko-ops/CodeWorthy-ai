# Copy to .env (or export in your shell) and adjust as needed.
DATABASE_URL=postgres://acme:acme@localhost:5432/acme_orders
PORT=3000

# Simulated PayFlow capture latency in milliseconds. PayFlow's sandbox is fast;
# production p99 has been observed above 4000ms (see TICKET.md).
PAYMENT_LATENCY_MS=50
